<h1 align="center"><%= "Category: " + @category.name  %></h1>
<div align="center">
  <% if logged_in? && current_user.admin? %>
    <span class="label"><%= link_to "Edit category name", edit_category_path(@category) %></span>
  <% end %>
  <span class="label"><%= link_to "Return to categories", categories_path %></span>
</div>
<div align="center">
  <%= will_paginate @category_articles %>
</div>
<%= render "articles/article", obj: @category_articles %>
<div align="center">
  <%= will_paginate @category_articles %>
</div>